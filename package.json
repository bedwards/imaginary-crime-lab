{
  "name": "crime-lab-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker API for Imaginary Crime Lab",
  "main": "worker.js",
  "type": "module",
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --env production",
    "tail": "wrangler tail --env production"
  }
}
